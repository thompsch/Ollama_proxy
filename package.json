{
  "name": "ollama-proxy",
  "version": "1.1.0",
  "private": true,
  "description": "Filtering reverse proxy for Ollama with optional MongoDB request logging",
  "main": "proxy.js",
  "dependencies": {
    "mongodb": "^6.20.0"
  }
}
